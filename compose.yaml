services:
  agent:
    build: .
    init: true
    restart: unless-stopped
    env_file:
      - .env
    environment:
      AGENT_DATA_DIR: /data
      CODEX_HOME: /data/codex
    volumes:
      - agent-data:/data
    stop_grace_period: 45s

volumes:
  agent-data:
